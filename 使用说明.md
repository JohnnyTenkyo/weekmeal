# 家味·一周菜单

记录每天三餐、明日解冻/预处理提醒、AI 健康菜谱推荐。两人跨设备共用一个云端数据库。

## 一、首次配置（约 10 分钟，只需做一次）

### 1. 建数据库（Supabase 免费）
1. 打开 https://supabase.com 注册，点 **New Project** 新建项目（区域选离你近的，密码自己记好）。
2. 项目建好后，左侧进 **SQL Editor** → New query。
3. 把本项目根目录的 `supabase-schema.sql` 文件内容整段复制进去，点 **Run**。看到 Success 即可。
4. 左侧 **Project Settings → API**，复制两个值：
   - `Project URL`
   - `anon public` key

### 2. 填到项目里
1. 把根目录的 `.env.local.example` 复制一份，改名为 `.env.local`。
2. 填入刚才那两个值：
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ....
   ```
3. 保存。

### 3. 填 AI Key
1. 启动后（见下），打开「设置」页。
2. 填入 API Key、API URL（兼容 OpenAI 格式，写到 /v1）、模型名，保存。
   - 默认偏好已设好：广式、不辣、忌菌菇、高血脂清淡、红肉每周≤2顿共100g，可自行调整。

## 二、启动
```bash
cd meal-planner
npm install      # 第一次或换电脑时
npm run dev      # 本地访问 http://localhost:3000
```

## 三、手机上用 + 两人共享
- 数据都存在 Supabase 云端，两人各自的设备打开同一个网址即可实时共享、都能编辑。
- 想随时随地用、像 App 一样放桌面：把项目部署到 **Vercel**（免费）：
  1. 把代码推到 GitHub。
  2. 在 vercel.com 导入该仓库，部署。
  3. 在 Vercel 项目的 Environment Variables 里加上 `.env.local` 那两个变量，重新部署。
  4. 手机浏览器打开 Vercel 给的网址 → 浏览器菜单「添加到主屏幕」，就有 App 图标了。

## 四、功能
- **本周**：7 天 × 三餐网格总览，点格子记录/查看。
- **想吃啥**：① AI 生成符合健康偏好的一周三餐，一键写入；② 输入现有食材，AI 反推能做的菜。
- **每一餐**：写菜名 → 「保存并让 AI 分析」→ 自动出做法 + 可勾选采购清单 + 自动把解冻/预处理排到前一天。
- **今明提醒**：今天要解冻/处理的清单（可手动加），以及明天三餐预览。

## 五、注意
- AI Key 存在数据库里、调用走服务端，不会暴露在浏览器。但 anon key 能读写数据库，所以**不要把网址公开分享**给无关的人。
